# Sprayer Description

This contains the URDF for the robot.

To preview it in RVIZ, build the project with `pixi run build`,
then run

```bash
pixi run -e sourced ros2 launch sprayer_description sprayer_description.launch.py
```
